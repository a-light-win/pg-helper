package web_server

func (w *WebServer) registerRoutes() error {
	// dbGroup := s.Router.Group("/api/v1/db")

	// TODO: Get task status
	// dbGroup.GET("/migrate/:taskId", s.Handler.MigrateDbStatus)

	return nil
}
